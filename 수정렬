#include <iostream>
#include <algorithm>

int main()
{
	int length;
	std::cin >> length;
	int *arr = new int[length];
	for (int i = 0; i < length; i++)
	{
		std::cin >> arr[i];
	}
	std::sort(arr, arr+length);
	for (int i = 0; i < length; i++)
		std::cout << arr[i]<<"\n";

	return 0;
}
